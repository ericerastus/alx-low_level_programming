#include <stdio.h>

/**
 * main - prints string inside printf
 *
 * Description: main function printing set of strings inside printf
 * Return: 0 if successful otherwise non-zro value
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
