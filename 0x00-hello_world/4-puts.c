#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)	/* Prints exactly
		 * Programming is like building a multilingual puzzle,
		 * followed by a new line
		 */
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
